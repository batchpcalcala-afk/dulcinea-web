import { defineConfig } from 'astro/config';

export default defineConfig({
  // Cambia esto por tu dominio definitivo, p. ej. https://dulcinea.batchpc-alcala.es
  site: 'https://dulcinea.example.com',
});
